{% extends this.layout %}

{% block breadcrumbs %}
	{% widget('zii.widgets.CBreadcrumbs', {'links' : {'Города' : ['index'], 0 : 'редактирование'}}) %}
{% endblock %}

{% block bigColumn %}

	<h1>Редактирование города: "{{ model.name_ru }}"</h1>

	{{ this.renderPartial('_form', {'model' : model}) }}

{% endblock %}

{% block sidebar %}
	{% set menu = [
			{'label' : 'К списку городов', 'url' : ['index']},
			{'label' : 'Добавить новый', 'url' : ['create']},
			{'label' : 'Просмотреть текущий', 'url' : {0 : 'view', 'id' : model.id}},
			{'label' : 'Удалить текущий', 'url' : 'javascript:void(0);', 'linkOptions' : {'submit' : {0 : 'delete', 'id' : model.id}, 'confirm' : 'Вы уверены, удалить этот город, все магазины будут удалены?'}}
	] %}
	{% beginWidget('zii.widgets.CPortlet', {'title' : 'Действия'}) %}
		{% widget('zii.widgets.CMenu', {'items' : menu, 'htmlOptions' : {'class' : 'operations'}}) %}
	{% endWidget %}
{% endblock %}